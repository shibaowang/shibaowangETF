<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Color x:Key="AppBackgroundColor">#050B14</Color>
    <Color x:Key="PanelTopColor">#071A2A</Color>
    <Color x:Key="PanelBottomColor">#06101B</Color>
    <Color x:Key="PanelAltColor">#081D2F</Color>
    <Color x:Key="BorderColor">#24415B</Color>
    <Color x:Key="BorderSoftColor">#18324A</Color>
    <Color x:Key="TextPrimaryColor">#E5EEF8</Color>
    <Color x:Key="TextSecondaryColor">#9CAFC3</Color>
    <Color x:Key="TextWeakColor">#64748B</Color>
    <Color x:Key="AccentBlueColor">#3B82F6</Color>
    <Color x:Key="AccentCyanColor">#22D3EE</Color>
    <Color x:Key="AccentRedColor">#EF4444</Color>
    <Color x:Key="AccentGreenColor">#84CC16</Color>
    <Color x:Key="AccentOrangeColor">#F59E0B</Color>

    <SolidColorBrush x:Key="AppBackgroundBrush" Color="{StaticResource AppBackgroundColor}" />
    <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource BorderColor}" Opacity="0.72" />
    <SolidColorBrush x:Key="BorderSoftBrush" Color="{StaticResource BorderSoftColor}" Opacity="0.74" />
    <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource TextPrimaryColor}" />
    <SolidColorBrush x:Key="TextSecondaryBrush" Color="{StaticResource TextSecondaryColor}" />
    <SolidColorBrush x:Key="TextWeakBrush" Color="{StaticResource TextWeakColor}" />
    <SolidColorBrush x:Key="AccentBlueBrush" Color="{StaticResource AccentBlueColor}" />
    <SolidColorBrush x:Key="AccentCyanBrush" Color="{StaticResource AccentCyanColor}" />
    <SolidColorBrush x:Key="AccentRedBrush" Color="{StaticResource AccentRedColor}" />
    <SolidColorBrush x:Key="AccentGreenBrush" Color="{StaticResource AccentGreenColor}" />
    <SolidColorBrush x:Key="AccentOrangeBrush" Color="{StaticResource AccentOrangeColor}" />
    <SolidColorBrush x:Key="ToolTipBackgroundBrush" Color="#071724" />
    <SolidColorBrush x:Key="ToolTipBorderBrush" Color="#2A6688" />
    <SolidColorBrush x:Key="ToolTipTextBrush" Color="#E8F4FF" />

    <LinearGradientBrush x:Key="PanelBrush" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="{StaticResource PanelTopColor}" Offset="0" />
        <GradientStop Color="{StaticResource PanelBottomColor}" Offset="1" />
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="CardBrush" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#081D30" Offset="0" />
        <GradientStop Color="#06121F" Offset="1" />
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="CardRedBrush" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#101923" Offset="0" />
        <GradientStop Color="#130E17" Offset="1" />
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="PoolBrush" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#081B24" Offset="0" />
        <GradientStop Color="#071C17" Offset="1" />
    </LinearGradientBrush>

    <Style TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
        <Setter Property="FontFamily" Value="Microsoft YaHei UI" />
        <Setter Property="LineStackingStrategy" Value="BlockLineHeight" />
    </Style>

    <Style TargetType="{x:Type ToolTip}">
        <Setter Property="Background" Value="{StaticResource ToolTipBackgroundBrush}" />
        <Setter Property="Foreground" Value="{StaticResource ToolTipTextBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource ToolTipBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="10,8" />
        <Setter Property="MaxWidth" Value="460" />
        <Setter Property="HasDropShadow" Value="True" />
        <Setter Property="Opacity" Value="0" />
        <Setter Property="TextElement.Foreground" Value="{StaticResource ToolTipTextBrush}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ToolTip}">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="5"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter TextElement.Foreground="{TemplateBinding Foreground}"
                                          MaxWidth="{TemplateBinding MaxWidth}" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <EventTrigger RoutedEvent="ToolTip.Opened">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                         From="0"
                                         To="1"
                                         Duration="0:0:0.14" />
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="PanelBorderStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource PanelBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource BorderSoftBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="8" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>

    <Style x:Key="IconButtonStyle" TargetType="Button">
        <Setter Property="Width" Value="34" />
        <Setter Property="Height" Value="28" />
        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}" CornerRadius="5">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#132B40" />
                            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
